# Cline 核心行为准则 (Core Rules)

## 一、 策略与效率 (Strategy & Efficiency)
1. **先思考，后行动 (Think Before Acting)**：在开始任何编码任务前，必须先在规划模式 (Plan Mode) 下输出思维链。列出涉及的文件、改动逻辑及潜在影响。
2. **批量操作 (Batch Processing)**：尽可能在一次 `write_to_file` 或 `replace_in_file` 中完成逻辑连贯的修改。避免为了微小的改动反复调用模型。
3. **精准检索 (Context Precision)**：
   - 优先使用 `grep_search` 或 `list_files` 定位代码，严禁盲目读取整个目录。
   - 除非必要，不要读取 `node_modules`, `dist`, `.git`, `build` 等无关目录。
   - 引用文件时，直接询问用户或根据已有上下文使用 `@` 路径，减少自动搜索步数。
4. **增量修改 (Incremental Edits)**：对于大文件，优先使用 `replace_in_file` 进行局部替换，而非重写整个文件，以节省输出 Token。

## 二、 内存银行规范 (Memory Bank - 提效关键)
*当上下文变长时，通过以下文件保持状态，以便在新任务中快速衔接，无需重读历史。*
1. **projectbrief.md**: 记录项目的核心目标和技术栈。
2. **activeContext.md**: 记录当前正在进行的任务、最近的决策和下一步计划。
3. **progress.md**: 记录已完成的功能和待办事项。
4. **每次重大改动后**，必须更新上述文件。当任务结束或 Token 消耗过高时，提醒用户启动 "New Task" 并依赖这些文件恢复上下文。

## 三、 代码质量与风格 (Code Standards)
1. **类型安全**：如果是 TypeScript 项目，必须严格定义 Interface 和 Type，禁止滥用 `any`。
2. **错误处理**：所有异步操作必须包含 `try-catch` 块，并有明确的错误提示。
3. **注释规范**：复杂的业务逻辑必须添加行内注释。新增的函数必须包含 JSDoc 注释。
4. **简洁至上**：代码应遵循 DRY (Don't Repeat Yourself) 原则，保持函数职责单一。

## 四、 工具调用约束 (Tool Usage)
1. **读取文件**：在读取文件前，先判断该文件是否已经在之前的对话中读取过（除非它已被修改）。重复读取是最大的 Token 浪费。
2. **终端操作**：
   - 运行测试命令前，确保已保存所有相关文件。
   - 只有在必要时才运行安装命令 (npm install/pip install)。
3. **确认机制**：
   - 对于涉及删除文件、清空数据库或修改敏感配置（如 .env）的操作，必须请求显式确认。
   - 在 Act 模式下，如果计划涉及超过 5 个文件的修改，必须先输出计划并等待用户输入 "Proceed"。

## 五、 响应风格 (Response Style)
1. **简洁回复**：非必要时，不要解释已知的编程常识。直接给出修改说明和代码块。
2. **进度汇报**：每完成计划中的一个步骤，简要标注 `[Done] 步骤描述`。